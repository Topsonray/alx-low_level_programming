#include "main.h"

/**
 * _isupper - checks if a letter is uppercase
 * @x: is the character to be checked
 * Return: 1 for uppercase or else 0
 */

int _isupper(int x)

{

	if (x >= 65 && x <= 90)
	{
	return (1);
	}
	return (0);
}

